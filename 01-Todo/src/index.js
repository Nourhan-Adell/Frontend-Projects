import ReactDOM from "react-dom/client";

import "./index.css";
import App from "./App";

// <App /> : is Called JSX (HTML in JS code)
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
